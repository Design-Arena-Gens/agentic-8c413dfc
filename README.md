# Agentic LLM

A tiny Next.js app featuring a built-in Markov language model with streaming chat. No external API keys required.

- `npm run dev` — start dev server
- `npm run build` — production build
- `npm start` — run production server

Deployed to Vercel as `agentic-8c413dfc`.
